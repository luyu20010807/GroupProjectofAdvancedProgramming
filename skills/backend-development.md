# Skill：FastAPI 领域服务开发

1. 在 Model 中只定义数据结构和关系。
2. 在 Service 中实现状态机、权限二次校验、事务、通知和审计。
3. Router 不直接拼装复杂状态变化。
4. 金额转换在边界层完成，内部统一使用分。
5. Web 使用 Session；小程序使用 Bearer Token；两者调用相同 Service。
6. 对资源不存在返回 404，对越权返回 403，对状态不允许返回 400。
7. 新增接口后同步更新 API 文档和测试。
