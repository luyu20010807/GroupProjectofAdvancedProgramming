# Skill：业务测试设计

每个业务点至少从六个维度设计用例：

- 正常：完整成功流程。
- 边界：数量、金额、时间的临界值。
- 异常：库存不足、对象不存在、参数为空。
- 状态：重复支付、已取消发货、已退款再次审核。
- 权限：换账号、改 URL、伪造 Token。
- 并发/幂等：双击提交、重复回调、多个管理员同时处理。

测试报告必须记录环境、数据、步骤、预期、实际、结论。Bug 修复要有失败用例和回归用例。
