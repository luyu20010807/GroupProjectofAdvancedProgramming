# 项目公共 Rules

## 1. 业务规则

- 一张订单只能属于一个商家，购物车结算必须按商家拆单。
- 订单状态只能通过 `order_service.transition_order` 变更。
- 售后状态只能通过 `refund_service` 中的函数变更。
- 同一订单不能同时存在多个进行中的售后。
- 商家审核售后必须写原因；平台仲裁必须写事实与规则依据。
- 所有核心状态变化必须写审计日志并通知相关角色。

## 2. 权限规则

- 用户只能访问自己的购物车、订单、售后和通知。
- 商家只能访问 `merchant_id` 等于自己的商品、订单和售后。
- 客服管理员只能处理售后与查看必要订单信息。
- 业务管理员可管理账号与商品治理。
- 技术管理员可查看审计信息，但无退款裁决权。
- 超级管理员拥有全部后台能力。

## 3. 编码规则

- Router 负责参数接收、登录校验和响应；复杂业务放在 Service。
- 金额使用整数“分”，禁止用浮点数直接存储人民币金额。
- 所有数据库写操作必须在成功后统一 commit；异常不得留下半完成数据。
- 公共函数必须有明确名称，不使用 `do_it`、`handle` 等含糊命名。
- 禁止提交 `.venv`、数据库、缓存、IDE 配置、密钥和构建产物。

## 4. 测试规则

- 新功能至少包含一个正常流程和一个失败流程。
- 权限功能必须包含直接构造 URL/API 的越权测试。
- 状态机必须测试非法跳转。
- 超时逻辑必须使用可控时间数据测试，不能真的等待 48 小时。
- 修复 Bug 后必须补回归测试。

## 5. Git 规则

- 禁止直接 push 到 `main`。
- 每个分支只聚焦一个模块或问题。
- Pull Request 必须写测试步骤和结果。
- 不使用 `git push --force` 覆盖公共分支。
- 不替其他成员提交代码；提交人必须与实际贡献者一致。
